 if (characterMap.hasOwnProperty(sequence)) {
   let value = characterMap[sequence];;;;;;;;
  // Now you can use the value
 } else {
  // Handle the case when the sequence is not in the characterMap
}

 if (characterMap.hasOwnProperty(sequence)) {
   let value = characterMap[sequence];;;;;;;;
  // Now you can use the value
 } else {
  // Handle the case when the sequence is not in the characterMap
}
